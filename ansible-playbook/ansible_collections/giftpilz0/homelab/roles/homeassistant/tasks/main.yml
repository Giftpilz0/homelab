---
- name: "Homeassistant: Create Podman config directory"
  become: true
  ansible.builtin.file:
    path: /etc/containers/containers.conf.d
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: "Homeassistant: Deploy podman proxy configuration"
  become: true
  ansible.builtin.template:
    src: "homeassistant-pod/proxy.conf.j2"
    dest: "/etc/containers/containers.conf.d/proxy.conf"
    owner: root
    group: root
    mode: "0644"
  when: homeassistant_proxy_enabled

- name: "Homeassistant: Delete quadlet directory"
  become: true
  become_user: "{{ podman_user }}"
  ansible.builtin.file:
    path: "/home/{{ podman_user }}/.config/containers/systemd/homeassistant-pod"
    state: absent

- name: "Homeassistant: Enable lingering"
  become: true
  ansible.builtin.command: loginctl enable-linger {{ podman_user }}
  changed_when: true

- name: "Homeassistant: Add user to dialout group for Zigbee device access"
  become: true
  ansible.builtin.user:
    name: "{{ podman_user }}"
    groups: dialout
    append: true

- name: "Homeassistant: Configure unprivileged port range for rootless containers"
  become: true
  ansible.posix.sysctl:
    name: net.ipv4.ip_unprivileged_port_start
    value: "80"
    sysctl_set: true
    sysctl_file: /etc/sysctl.d/99-rootless-ports.conf
    reload: true

- name: "Homeassistant: Create quadlet directory"
  become: true
  become_user: "{{ podman_user }}"
  ansible.builtin.file:
    path: "/home/{{ podman_user }}/.config/containers/systemd"
    state: directory
    owner: "{{ podman_user }}"
    group: "{{ podman_user }}"
    mode: "0755"

- name: "Homeassistant: Create SPIRE token directory"
  become: true
  become_user: "{{ podman_user }}"
  ansible.builtin.file:
    path: "/home/{{ podman_user }}/.config/containers/systemd/homeassistant-pod/token"
    state: directory
    owner: "{{ podman_user }}"
    group: "{{ podman_user }}"
    mode: "0700"
  when: homeassistant_spire_enabled

- name: "Homeassistant: Create directory structure"
  become: true
  become_user: "{{ podman_user }}"
  ansible.builtin.file:
    path: "/home/{{ podman_user }}/.config/containers/systemd/homeassistant-pod/{{ item.path }}"
    state: directory
    owner: "{{ podman_user }}"
    group: "{{ podman_user }}"
    mode: "{{ item.mode }}"
  with_community.general.filetree: "templates/homeassistant-pod/"
  when: item.state == 'directory'

- name: "Homeassistant: Template quadlet files"
  become: true
  become_user: "{{ podman_user }}"
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "/home/{{ podman_user }}/.config/containers/systemd/homeassistant-pod/{{ item.path }}"
    owner: "{{ podman_user }}"
    group: "{{ podman_user }}"
    mode: "{{ item.mode }}"
  with_community.general.filetree: "templates/homeassistant-pod/"
  when: item.state == 'file'

- name: "Homeassistant: Copy SPIRE join token"
  become: true
  become_user: "{{ podman_user }}"
  ansible.builtin.copy:
    src: "{{ podman_user }}/.config/containers/systemd/spire-pod/tokens/homeassistant.token"
    dest: "/home/{{ podman_user }}/.config/containers/systemd/homeassistant-pod/token"
    owner: "{{ podman_user }}"
    group: "{{ podman_user }}"
    mode: "0600"
    remote_src: true
  when: homeassistant_spire_enabled
  failed_when: false

- name: "Homeassistant: Create SPIRE socket volume"
  containers.podman.podman_volume:
    name: homeassistant-spire-sockets
    state: present
  when: homeassistant_spire_enabled

- name: "Homeassistant: systemd daemon reload"
  become: true
  become_user: "{{ podman_user }}"
  ansible.builtin.systemd:
    daemon_reload: true
    scope: user

- name: "Homeassistant: Start pod service"
  become: true
  become_user: "{{ podman_user }}"
  ansible.builtin.systemd:
    name: homeassistant-pod.service
    enabled: true
    state: started
    scope: user

- name: "Homeassistant: Enable podman-auto-update timer"
  become: true
  become_user: "{{ podman_user }}"
  ansible.builtin.systemd:
    name: podman-auto-update.timer
    enabled: true
    state: started
    scope: user
